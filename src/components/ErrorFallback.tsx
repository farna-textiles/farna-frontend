// If something goes wrong with lazy loading, fall back to this component

const ErrorFallback = () => {
  return <h1>Error! Please refresh or try again later.</h1>;
};

export default ErrorFallback;
